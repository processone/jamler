type loglevel = [ Notice <json name="notice">
                 | Debug <json name="debug">
		 | Error <json name="error"> ]

type config = {
  hosts : string list;
  ~loglevel <ocaml default="`Notice"> : loglevel;
}

(* Local Variables: *)
(* mode: tuareg     *)
(* End:             *)
